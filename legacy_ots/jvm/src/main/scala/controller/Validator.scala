package controller

import controller.db.User
import extensionsInterface.VerificationResult

object Validator {

  /*def validateAnswer(answerIdHex:String, answer:String, user: User) : VerificationResult = {

  }

  def validateAnswerUnsafe(alias:String, seed:Int, answer:String) : VerificationResult = {
    val pt = TemplatesRegistry.getProblemTemplate(alias)
    pt.map{ pt => pt.validateAnswer(seed, answer)}


  }*/

}
