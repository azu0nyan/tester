package controller

object Permissions {

  /*sealed trait Permission
  sealed trait ReadPermission extends Permission
  sealed trait WritePermission extends Permission
  sealed trait ReadWritePermission extends ReadPermission with WritePermission



  trait ReadUserInfoPermission extends ReadPermission {
    def allowByHexUid:String => Boolean
  }

  sealed trait PermitableAction

  case class ReadUserInfo(uid:String)
*/
}
