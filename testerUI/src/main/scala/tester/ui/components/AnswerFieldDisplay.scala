package tester.ui.components

import typings.antd.components.Input
import typings.antd.components.Input.TextArea
import scala.scalajs.js
import slinky.core.*
import slinky.web.html.*
import slinky.core.facade.Hooks.{useEffect, useState}
import slinky.core.facade.ReactElement
import typings.StBuildingComponent
import typings.antDesignIcons.components.AntdIcon
import typings.antd.antdStrings.dark
import typings.antd.components.*
import typings.antd.libMenuMenuItemMod.MenuItem
import viewData.PartialCourseViewData

object AnswerFieldDisplay {


//  def textField(currentAnswer: String, question: String, lines: Int,  submit: String => Unit) =
//    val body = if (lines == 1) Input.value(currentAnswer)
//    else TextArea.rows(lines).value(currentAnswer)
//    if (question.nonEmpty) Card.title(question)(
//      body
//    ) else Card(body)
}
